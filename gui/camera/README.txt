This directory handles code related to showing images received from the 
camera(s). It depends heavily on the gui/imageViewer code.

viewPanel.py: Handles a single camera display.
window.py: Arranges multiple ViewPanels, and handles things when cameras
  are added/removed.
